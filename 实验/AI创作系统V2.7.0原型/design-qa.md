# 产品演示替换 HTML 原型验收记录

## 对比对象

- 设计依据：`AI 创作/我的任务.html` 的任务卡结构，以及 `AI 创作/人工审核页.html` 的任务上下文、流程节点、工作台和确认动作框架。
- 实现文件：`产品演示替换-工作流使用页.html`。
- 目标状态：我的任务列表底部的待处理卡片，点击“去处理”进入产品演示替换页。

## 已完成的静态检查

1. 已从 HTML 中抽取内嵌脚本，并通过 `node --check --input-type=commonjs`。
2. 已检查产品主链路的可见控件和事件绑定：`去处理`、返回我的任务、候选视频上传、候选视频预览/删除、合成结果预览、确认并继续任务。
3. 已检查任务入口、处理页映射规则和确认后的任务卡状态回写，均在同一原型文件中有明确实现。
4. 已运行 `git diff --check`，未发现空白或补丁格式问题。

## 关键交互覆盖

| 场景 | 预期实现 |
| --- | --- |
| 任务入口 | “我的任务”列表最下方出现待处理任务卡；“去处理”切换到产品演示替换页。 |
| 逐段处理 | 当前片段选择候选视频，或选择“保留原片段”。 |
| 候选视频 | 可上传、预览、删除；已被某片段使用时禁止删除。 |
| 合成预览 | 两个片段都有结论后，才允许预览合成结果。 |
| 继续任务 | 回到任务页，当前节点改为已完成，任务提示变为“视频合成正在运行”。 |

## 浏览器验收状态

- 原型截图：未生成。
- 视口：未生成。
- 原因：当前浏览器安全策略阻止访问本地 `file://` 页面；未使用规避方式。
- 控制台错误：未能在浏览器中采集。

## 结论

静态结构与脚本检查通过；浏览器视觉与真实点击验收**受限**，因此本记录不将页面视觉标记为通过。

final result: blocked
